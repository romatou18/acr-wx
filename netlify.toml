[build]
  command = "bash scripts/build.sh"
  functions = "functions"

[functions."weather-bot"]
  schedule = "* * * * *"

[functions."weather-api"]
  # HTTP GET /weather-api?lat=<lat>&lon=<lon>
  # Returns the same compressed report sent to the Garmin device.
