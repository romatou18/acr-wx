# Local Netlify folder
.netlify
/functions/weather-bot

# Local test logs
/logs/

# Captured Garmin email samples (scratch inputs, not part of the app)
/*.html

# HAR captures — contain session cookies/tokens, never commit
/*.har

# Environment / secrets — never commit
.env

# Raw Garmin page captures (scratch; hold real session tokens + saved _files/).
# The sanitized golden fixture lives at cmd/bot/testdata/ and IS committed.
/testdata/
